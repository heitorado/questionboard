$('#<%= params[:id] %>').find('.card').css('background-color', '#69f0ae');

$('#<%= params[:id] %>').fadeOut(500, function() { 
    $(this).remove();
    // Display toastr success notification
    M.toast({
            html: "Marked as answered!"
            });
});


    
